import 'package:flutter/material.dart';

class  e_mesurePage extends StatelessWidget {


  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('E_mesure'),
      centerTitle: true,
      backgroundColor: Colors.blueAccent,
    ),
  );
}